# File Naming Convention Rule

## Naming Strategy

- Kebab-case only: All files must be named using kebab-case (e.g., user-profile.ts, api-handler.ts).
- Prohibited Formats: Never use PascalCase (UserProfile.ts), camelCase (userProfile.ts), or snake_case (user_profile.ts).
